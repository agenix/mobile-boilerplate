# mobile-boilerplate


## Install
```
  npm i -g expo-cli
  cd agenix/ios
  pod install
```
## Run
```
  cd agenix
  yarn android (ensure you have a device connected or an emulator running)
  yarn ios
```